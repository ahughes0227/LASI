# Experiments

One directory per experiment, `20_work/experiments/<experiment_id>/`, holding its
hypothesis, config, inputs, results, artifacts, critique, and invalidation record.

Metrics and produced binaries live in the structured and artifact systems; these
directories record the readable interpretation and the references.
