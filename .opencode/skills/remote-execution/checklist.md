Remote Execution Checklist
==========================

1. Verify `Decision Record` authorizes the remote run when required.
2. Validate remote host profile and tooling availability.
3. Execute run and capture stdout/stderr and exit code.
4. Upload logs and artifacts to the artifact store and record URIs.
5. Produce `remote_run_record.md` with provenance and status.
