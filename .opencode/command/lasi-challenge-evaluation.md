---
description: Run the governed local challenge evaluation workflow through LASI services.
agent: lasi-coordinator
---

Route `$ARGUMENTS` through the reusable LASI benchmark services. Require a local challenge brief, explicit file paths, target/ID/schema decisions, privacy and budget policy, and a durable ExperimentPlan plus DecisionRecord before any model run. Use only local registered tools in benchmark isolation; do not search the web, contact challenge sites, inspect hidden labels, or execute arbitrary commands. Return typed handoffs and fixed report artifacts, recording blocked or missing states explicitly.
