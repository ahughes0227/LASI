---
description: Create a governed dataset manifest for a new LASI dataset version.
agent: dataset-engineer
---

Use the dataset-intake skill for `$ARGUMENTS`. Use `lasi.datasets.service.load_parquet_manifest` and `validate_dataset` where applicable to capture project and dataset version identity, source and ownership, checksums, privacy tags, and provenance. Do not modify labels or authoritative storage. Identify missing inputs and approval needs.
