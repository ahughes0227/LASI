requirements:
  - declared input and output contracts
  - no unrequested side effects
  - explicit provenance and failure states
cases:
  - id: approved-plan-execution-boundary
    input: experiment/plan
    expected: experiment/result

