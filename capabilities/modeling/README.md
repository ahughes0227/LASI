# Modeling

Run only approved experiment plans with declared inputs and isolated, traceable results.

This fixed-shell package converts `system/capabilities/modeling.md`. It remains a draft until validation and governed registration succeed.

