# Current State

Project `mercury` completed governed local intake. The authorized Kaggle CLI acquisition completed on 2026-08-13: CLI `2.2.4` listed the seven official challenge files and retrieved the official archive to `artifacts/kaggle-download/store-sales-time-series-forecasting.zip`. The seven CSV assets were safely extracted, copied into a read-only challenge workspace, hashed, and schema-inspected; see `10_context/local-intake.md`.

The project owner granted standing authorization to continue Mercury autonomously. The divergent direct multi-horizon GBDT improved local RMSLE to `0.4609337296` and Kaggle public score to `0.49942` (submission `55489163`), beating recursive seasonal-naive (`0.6170404161` local / `0.52245` public). Two Kaggle submissions remain today. The project will continue with increasingly novel local branches; public scores remain external evidence only, with no private score or production outcome available.

The initial intake handoff at `10_context/challenge_intake_handoff.md` is superseded by `10_context/local-intake.md`. No challenge execution has occurred.

On 2026-08-13, the LASI service import boundary that previously prevented benchmark-harness test collection was repaired and verified. This is harness-maintenance evidence only; it does not change challenge authorization or satisfy the missing local-input dependency.

The owner approved work on a benchmark-safe component containment boundary. Revision 001 of the hurdle component removed its arbitrary `train_path` configuration input and requires the declared `train_panel_csv` artifact input; synthetic tests passed. Independent review confirms that this alone does not establish protected execution containment. The component remains unregistered and no challenge execution has occurred.

Revision 002 added Python-level immutable registration binding, pre-write path allowlists, audit-hook filesystem/network/subprocess denial, CPU/wall-time enforcement, and explicit Darwin fail-closed behavior where in-process hard memory enforcement is unavailable. Focused tests passed (16), but independent review returned `request_revision`: the request hash is unreconciled, dependency approval evidence is incomplete, and native dependencies require a locally permitted OS/process-isolated backend with enforceable memory limits. This is a correctable engineering and evidence gap, not a human escalation. No official challenge data or Kaggle quota was consumed by this containment work.

Revision 003 reconciled the immutable component hash and lockfile-pinned dependency evidence. The Darwin host has `sandbox-exec` for child filesystem/network/process isolation, but no verified hard memory limit for native dependencies; Docker is installed but its daemon is unavailable. Local protected execution therefore remains fail-closed. An approved process- or OS-isolated host with hard memory enforcement is now required before this project-local component can be reviewed for protected registration; no challenge execution has occurred.

Independent review of revision 003 recorded `request_revision`, not authorization. The required backend cannot be supplied inside Mercury's current `local_only` / `remote_execution: blocked_by_benchmark_isolation` boundary: a suitable process- or OS-isolated host would change the approved execution and data-transfer boundary, while weakening the memory containment requirement would change benchmark protection. This is a true human-discretion boundary. No component was registered, no experiment plan was allowed to execute, and no Kaggle quota was consumed.

The owner subsequently approved use of a suitable process- or OS-isolated execution host with enforceable hard memory limits, subject to recorded benchmark and privacy controls. This authorizes evaluation of that host category; it does not create a host profile, permit challenge-data transfer, amend the `local_only` challenge privacy setting, or authorize the currently local-only draft component. A bounded configuration review found no concrete enabled approved host profile, no transfer policy, no remote experiment plan or decision record, and no revised remote-capable component contract. No remote validation, data staging, challenge execution, or Kaggle submission occurred.
