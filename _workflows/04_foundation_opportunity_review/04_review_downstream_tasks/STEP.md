# Step 04 — Review Downstream Tasks

Purpose
: Map downstream consumers and measure the reuse value of shared features or labels.

Expected output
- `downstream_tasks.md`

Skill
- `experiment_planning`, `knowledge_curation`
